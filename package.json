{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "bunx @biomejs/biome format --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@biomejs/biome": "^1.4.1",
    "eslint": "^8.48.0",
    "turbo": "latest"
  },
  "name": "mailnow",
  "packageManager": "bun@1.0.6",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ]
}
